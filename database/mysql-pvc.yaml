apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    template: mysql-persistent-template
  name: mysql
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
